import { Platform } from '@angular/cdk/platform';

export const mockPlatform: Platform = {
    ANDROID: false,
    BLINK: false,
    EDGE: false,
    FIREFOX: false,
    IOS: false,
    isBrowser: true,
    SAFARI: false,
    TRIDENT: false,
    WEBKIT: false,
} as Platform;
